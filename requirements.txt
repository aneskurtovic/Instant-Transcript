# Web framework + server
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9

# YouTube audio download (keep updatable — YouTube changes break old pins)
yt-dlp>=2024.0.0

# Local transcription (CTranslate2 backend, GPU or CPU)
faster-whisper>=1.0.0

# Config + rate limiting
python-dotenv>=1.0
slowapi>=0.1.9
